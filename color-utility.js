export function sayHello() {
    console.log('Hello, color utility package works!');
}

// 1 - setOpacity(hexColor, opacity) -> return hexColor con applicata opacity


// 2 - fromColorStringToRGB(stringColor - deve essere hex. ottimale, deve essere il nome del colore, per esempio "red") -> return colore rgb


// 3 - getContrastingColor(colorString) -> return string